{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "environment": {
      "value": "${ENVIRONMENT:dev}"
    },
    "functionAppName": {
      "value": "colectica-mcp"
    },
    "keyVaultId": {
      "value": "${KEY_VAULT_ID}"
    },
    "managedIdentityId": {
      "value": "${MANAGED_IDENTITY_ID:}"
    }
  }
}
